@model object
@(
Html.Kendo().ComboBoxFor(m => m)
    .Placeholder("Day Types")
    .DataTextField("DayType")
    .DataValueField("Id")
    .Filter(FilterType.Contains)
    .AutoBind(true)
    .MinLength(3)
    .Template("<span>#: data.DayType#</span>")
    .ValuePrimitive(true)
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Url("/TrnDtr/Detail?handler=DayTypes");
        })
        .ServerFiltering(false);
    })
)